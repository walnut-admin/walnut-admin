node_modules
.DS_Store
*.local
*.zip
release-notes.md

# Turborepo
.turbo

# Build outputs
dist/
dist-*/
dev-dist/

# TypeScript
*.tsbuildinfo

# Auto-generated
# ⚠️ 与下面 `report/` 那条同一个坑：带斜杠的模式**锚定到本文件所在目录**。原来这里写的是
# `types/auto-import.d.ts` —— 锚定的是**仓库根**（那里没有 types/），而文件其实在
# `apps/admin/types/` 下 ⇒ 两条规则**一条都没生效**（`git check-ignore` 无输出），
# 于是它们既没被忽略、又被 git 跟踪，每次 `pnpm dev` 重写都把工作区弄脏。
# 现在生成物统一落到 `apps/admin/types/generated/`（生成器 `build/generate/genTypeDeclarations.ts`，
# 由 `pretypes:check` 调用），规则写成**从仓库根出发的路径**，锚定语义与预期一致。
apps/admin/types/generated/

# Build generated
build/_generated/*.ts
build/_generated/*.json
build/_generated/*.log

# 镜像构建的 staging 目录（pnpm deploy --prod 产物 / 前端 dist 拷贝）
# CI 里改为落在工作区之外（见 release.yml），这里是本地构建用的默认位置
build/image/

# Environment
.env.keys
env-local/
deploy/env/
deploy/nginx/certs/

# Reports (keep .md files)
# ⚠️ 必须写 `**/`：带斜杠的模式是**锚定到本文件所在目录**的，`report/*` 只匹配仓库根的
# `report/`（该目录并不存在），而实际产出在 `apps/admin/report/`（`types:check:log` 写的
# tsc.log）—— 那只会在每个跑过该脚本的机器上留下一个未跟踪文件。
**/report/*
!**/report/*.md

# Git hooks（lefthook 运行期缓存与本地覆写；钩子内容本身在 lefthook.yml，进仓库）
.lefthook/
.lefthook-local.yml

# 发版
# ⚠️ `.changeset/` 其余内容（意图 *.md、ledger.yaml、README.md）**必须提交**，不要整体忽略。
# 这里只忽略本机状态与 pnpm 的寄存区：
.changeset/.release-state.json
# `.changeset/changelogs/` 是 pnpm 的 changelog 寄存区，只有 publish 时才有人读，
# 而本仓 `versioning.changelog.storage: registry` + 从不 publish ⇒ 它没有消费者，
# 发版脚本消费完连目录一起清掉；留这条是兜底（中断时可能留下残留）。
.changeset/changelogs/

# VitePress 构建临时目录
apps/docs/.vitepress/.temp/
apps/docs/.vitepress/dist/
apps/docs/.vitepress/cache/

# Legacy
config/husky/*

# Server
apps/server/env-local/
apps/server/logs/
apps/server/dist/

# AI agent workspace (ZCode plans, scratch)
.zcode/

# VitePress (docs) — .vitepress/dist already covered by dist/ above
**/.vitepress/cache
apps/docs/nginx/*