/** Token storage key names — used by both frontend (localStorage) and backend (JWT) */
export const WalnutAdminConstAppTokenKey = {
  ACCESS: 'WALNUT_ACCESS_TOKEN',
  REFRESH: 'WALNUT_REFRESH_TOKEN',
} as const
